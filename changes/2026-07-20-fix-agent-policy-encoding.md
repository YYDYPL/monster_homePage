# AGENT.md 约束文档修复

## 改动日期

2026-07-20

## 改动内容

- 在项目根目录创建并规范化 `AGENT.md`。
- 写入项目协作约束：每次对项目进行更改，都必须在根目录的 `changes` 文件夹中新增一份独立的 Markdown 改动文档。
- 使用 UTF-8 编码保存，修复原文件中的中文乱码。

## 影响范围

本次仅调整项目协作规则文档，不涉及应用代码、构建配置或运行时行为。