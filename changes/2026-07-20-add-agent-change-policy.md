# 新增项目改动文档约束

- **变更日期**：2026-07-20
- **变更类型**：项目协作规范

## 变更目的

确保每次项目更改都有清晰、可追溯的文档记录。

## 变更内容

1. 在项目根目录新增 `AGENT.md`。
2. 规定每次对项目进行更改时，都必须在根目录的 `changes` 文件夹中新增一份独立的 Markdown 改动文档。
3. 新增 `changes` 文件夹，并使用本文档记录本次规范变更。

## 影响范围

本次变更仅涉及项目协作规范，不涉及业务代码、运行配置或数据库结构。