import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {Markdown} from "@/components/markdown";import {getProject} from "@/lib/api";export const dynamic="force-dynamic";export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const response=await getProject(slug);return response.success?{title:response.data.name,description:response.data.summary}:{title:"项目"}}export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const response=await getProject(slug);if(!response.success||!response.data)notFound();const project=response.data;const tech=(project.techStack||"").split(",").map(x=>x.trim()).filter(Boolean);return <><section className="page-hero"><div className="container"><p className="eyebrow">Project / {project.status}</p><h1>{project.name}</h1><p className="hero-description">{project.summary}</p><div className="hero-actions">{project.demoUrl&&<a className="button primary" href={project.demoUrl} target="_blank" rel="noreferrer">在线演示 ↗</a>}{project.repoUrl&&<a className="button" href={project.repoUrl} target="_blank" rel="noreferrer">查看源码 ↗</a>}<Link className="button ghost" href="/projects">返回项目列表</Link></div></div></section><section className="list-section"><div className="container article-layout"><div><Markdown content={project.description||"这个项目的详细复盘正在整理中。"}/></div><aside className="article-aside"><h3>项目技术栈</h3><div className="tech-row">{tech.map(item=><span key={item}>{item}</span>)}</div><div className="share-line"><h3>项目周期</h3><p style={{color:"var(--text-3)",fontSize:12,lineHeight:1.8}}>{project.startDate||"未设置"} — {project.endDate||"至今"}</p></div></aside></div></section></>}
