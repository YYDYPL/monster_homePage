import type {Metadata} from "next";import {PageHero} from "@/components/ui";import {getFullProfile} from "@/lib/profile-api";export const dynamic="force-dynamic";export const metadata:Metadata={title:"友情链接",description:"优质技术网站与常用资源。"};export default async function LinksPage(){const profile=await getFullProfile();const links=profile.links?.links||[];return <><PageHero eyebrow="Bookmarks" title="链接与资源" description="我经常使用、长期关注或认为值得推荐的技术站点。"/><section className="list-section"><div className="container simple-grid">{links.map((link,index)=><a className="simple-card link-card" href={link.url} target="_blank" rel="noreferrer" key={link.name}><span className="number">{String(index+1).padStart(2,"0")} / LINK</span><h2>{link.name} ↗</h2><p>{link.description}</p></a>)}</div></section></>}
