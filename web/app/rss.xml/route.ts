import {getPosts} from "@/lib/api";const escapeXml=(value:string)=>value.replace(/[<>&'"]/g,char=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"})[char]!);export async function GET(){const base=process.env.SITE_URL||"http://localhost:3000";const response=await getPosts(1,50);const posts=response.success?response.data.items:[];const xml=`<?xml version="1.0" encoding="UTF-8" ?><rss version="2.0"><channel><title>Monster · 个人技术空间</title><link>${base}</link><description>Java、Spring、数据库与工程实践</description><language>zh-CN</language>${posts.map(post=>`<item><title>${escapeXml(post.title)}</title><link>${base}/blog/${encodeURIComponent(post.slug)}</link><guid>${base}/blog/${encodeURIComponent(post.slug)}</guid><description>${escapeXml(post.summary||"")}</description><pubDate>${new Date(post.publishedAt||post.updatedAt).toUTCString()}</pubDate></item>`).join("")}</channel></rss>`;return new Response(xml,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, max-age=3600, s-maxage=3600"}})}
