import Link from "next/link";import {fallbackNotes,fallbackPosts,fallbackProjects,fallbackSiteConfig,getNotes,getPosts,getProjects,getSiteConfig} from "@/lib/api";import {ArrowIcon,NoteCard,PostCard,ProjectCard,SectionHeading} from "@/components/ui";export const dynamic="force-dynamic";
export default async function HomePage(){const[postResponse,noteResponse,projectResponse,configResponse]=await Promise.all([getPosts(1,4),getNotes(1,4),getProjects(1,3),getSiteConfig()]);const config=configResponse.success?configResponse.data:fallbackSiteConfig;const posts=postResponse.success?postResponse.data.items:fallbackPosts;const notes=noteResponse.success?noteResponse.data.items:fallbackNotes;const projects=projectResponse.success?projectResponse.data.items:fallbackProjects;return <><section className="hero"><div className="container hero-grid"><div className="hero-copy"><p className="eyebrow">Hello, world</p><h1>构建系统，<span className="outline">沉淀知识，</span><span className="accent">持续进化。</span></h1><p className="hero-lead">你好，我是 <strong>{config.ownerName}</strong>，{config.headline}。这里是我的技术博客、知识库、项目档案和线上实验场。</p><div className="hero-actions"><Link className="button primary" href="/projects">探索我的项目 <ArrowIcon/></Link><Link className="button ghost" href="/about">了解更多</Link></div><div className="hero-meta"><span><i/>开放交流</span><span>{config.headline}</span><span>Based in {config.location}</span></div></div><div className="terminal-card" aria-label="个人技术栈终端卡片"><div className="terminal-bar"><i/><i/><i/><span>monster@dev:~</span></div><div className="terminal-content"><p><span className="prompt">$</span> whoami</p><p className="value">computer-science-developer</p><br/><p><span className="prompt">$</span> cat stack.json</p><p>{`{`}</p><p>&nbsp;&nbsp;&quot;backend&quot;: <span className="value">&quot;Java / Spring Boot&quot;</span>,</p><p>&nbsp;&nbsp;&quot;frontend&quot;: <span className="value">&quot;Next.js / React&quot;</span>,</p><p>&nbsp;&nbsp;&quot;database&quot;: <span className="value">&quot;PostgreSQL&quot;</span>,</p><p>&nbsp;&nbsp;&quot;infra&quot;: <span className="value">&quot;Docker / Linux&quot;</span></p><p>{`}`}</p><br/><p className="comment"># always building something useful</p><p><span className="prompt">$</span><span className="blink"/></p></div></div></div></section><section className="stats-strip"><div className="container stats-grid"><div className="stat-item"><strong>{postResponse.success?postResponse.data.total:posts.length}+</strong><span>技术文章</span></div><div className="stat-item"><strong>{noteResponse.success?noteResponse.data.total:notes.length}+</strong><span>知识笔记</span></div><div className="stat-item"><strong>{projectResponse.success?projectResponse.data.total:projects.length}+</strong><span>项目实践</span></div><div className="stat-item"><strong>∞</strong><span>持续学习</span></div></div></section><section className="section"><div className="container"><SectionHeading eyebrow="Latest writing" title="最近在写" href="/blog"/><div className="content-grid">{posts.slice(0,4).map((post,index)=><PostCard post={post} featured={index===0&&posts.length>2} key={post.id}/>)}</div></div></section><section className="section alt"><div className="container"><SectionHeading eyebrow="Knowledge base" title="知识片段" href="/notes"/><div className="note-list">{notes.slice(0,4).map(note=><NoteCard note={note} key={note.id}/>)}</div></div></section><section className="section"><div className="container"><SectionHeading eyebrow="Selected work" title="精选项目" href="/projects"/><div className="content-grid three">{projects.slice(0,3).map(project=><ProjectCard project={project} key={project.id}/>)}</div></div></section><section className="section alt"><div className="container"><div className="section-heading"><div><p className="eyebrow">Let&apos;s connect</p><h2>有想法？一起聊聊。</h2></div><Link className="button primary" href="/contact">联系我 <ArrowIcon/></Link></div></div></section></>}
