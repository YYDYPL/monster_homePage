import type { MetadataRoute } from "next";
import { getNotes, getPosts, getProjects } from "@/lib/api";
import { labTools } from "@/lib/lab-tools";
const base=process.env.SITE_URL||"http://localhost:3000";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const staticPaths=["","/about","/blog","/notes","/projects","/lab",...labTools.map(tool=>`/lab/${tool.id}`),"/resume","/uses","/links","/search","/contact"];const [posts,notes,projects]=await Promise.all([getPosts(1,100),getNotes(1,100),getProjects(1,100)]);return [...staticPaths.map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:.7})),...(posts.success?posts.data.items.map(item=>({url:`${base}/blog/${item.slug}`,lastModified:new Date(item.updatedAt),changeFrequency:"monthly" as const,priority:.8})):[]),...(notes.success?notes.data.items.map(item=>({url:`${base}/notes/${item.slug}`,lastModified:new Date(item.updatedAt),changeFrequency:"weekly" as const,priority:.7})):[]),...(projects.success?projects.data.items.map(item=>({url:`${base}/projects/${item.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.7})):[])]}
