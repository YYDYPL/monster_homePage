import type {Metadata} from "next";import Link from "next/link";import {EmptyState,PageHero,PostCard} from "@/components/ui";import {fallbackPosts,getPosts} from "@/lib/api";export const dynamic="force-dynamic";export const metadata:Metadata={title:"技术博客",description:"Java、Spring、数据库、架构与工程实践文章。"};export default async function BlogPage({searchParams}:{searchParams:Promise<{page?:string}>}){const params=await searchParams;const page=Math.max(1,Number(params.page)||1);const response=await getPosts(page,10);const data=response.success?response.data:{items:page===1?fallbackPosts:[],page,size:10,total:fallbackPosts.length,totalPages:1};return <><PageHero eyebrow="Writing" title="技术博客" description="记录技术选择背后的思考、真实项目中的取舍，以及那些值得重复使用的工程经验。"><div className="filter-bar"><Link className="active" href="/blog">全部文章</Link>{["Java","Spring","数据库","架构","DevOps"].map(x=><span className="filter-chip" key={x}>{x}</span>)}</div></PageHero><section className="list-section"><div className="container"><div className="content-grid">{data.items.length?data.items.map(post=><PostCard post={post} key={post.id}/>):<EmptyState/>}</div>{data.totalPages>1&&<nav className="pagination" aria-label="博客分页">{page>1&&<Link href={`/blog?page=${page-1}`}>←</Link>}<span className="current">{page}</span>{page<data.totalPages&&<Link href={`/blog?page=${page+1}`}>→</Link>}</nav>}</div></section></>}
