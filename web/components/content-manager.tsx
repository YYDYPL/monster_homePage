"use client";
import {FormEvent,useCallback,useEffect,useRef,useState} from "react";import {adminGet,adminMutation,type AdminPage,type EditableItem} from "@/lib/admin-api";import {handleContentPaste} from "@/lib/paste-image";
type Mode="posts"|"notes"|"projects";type FormState={id?:string;title:string;name:string;slug:string;summary:string;content:string;description:string;coverImageUrl:string;tags:string;series:string;category:string;techStack:string;status:string;featured:boolean;repoUrl:string;demoUrl:string;imageUrl:string;startDate:string;endDate:string};
const labels={posts:{title:"文章管理",description:"创建、编辑和发布技术博客。",newLabel:"新建文章"},notes:{title:"笔记管理",description:"维护知识分类与技术笔记。",newLabel:"新建笔记"},projects:{title:"项目管理",description:"展示项目、技术栈与项目复盘。",newLabel:"新建项目"}};
function empty(mode:Mode):FormState{return{id:undefined,title:"",name:"",slug:"",summary:"",content:"",description:"",coverImageUrl:"",tags:"",series:"",category:"",techStack:"",status:mode==="projects"?"EXPERIMENTAL":"DRAFT",featured:false,repoUrl:"",demoUrl:"",imageUrl:"",startDate:"",endDate:""}}function toForm(item:EditableItem,mode:Mode):FormState{return{id:item.id,title:item.title||"",name:item.name||"",slug:item.slug||"",summary:item.summary||"",content:item.content||"",description:item.description||"",coverImageUrl:item.coverImageUrl||"",tags:(item.tags||[]).join(", "),series:item.series||"",category:item.category||"",techStack:item.techStack||"",status:item.status||(mode==="projects"?"EXPERIMENTAL":"DRAFT"),featured:Boolean(item.featured),repoUrl:item.repoUrl||"",demoUrl:item.demoUrl||"",imageUrl:item.imageUrl||"",startDate:item.startDate||"",endDate:item.endDate||""}}const split=(value:string)=>value.split(",").map(x=>x.trim()).filter(Boolean);
export function ContentManager({mode}:{mode:Mode}){const[items,setItems]=useState<EditableItem[]>([]);const[form,setForm]=useState<FormState>(()=>empty(mode));const[editing,setEditing]=useState(false);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[pasteUploading,setPasteUploading]=useState(false);const[message,setMessage]=useState("");const editorRef=useRef<HTMLTextAreaElement>(null);const config=labels[mode];useEffect(()=>{const el=editorRef.current;if(!el)return;const handler=(e:ClipboardEvent)=>{handleContentPaste(e,setPasteUploading)};el.addEventListener("paste",handler);return()=>el.removeEventListener("paste",handler)},[mode]);const load=useCallback(async()=>{setLoading(true);try{const response=await adminGet<AdminPage>(`/api/admin/${mode}?page=1&size=100`);setItems(response.data.items)}catch(error){setMessage(error instanceof Error?error.message:"加载失败")}finally{setLoading(false)}},[mode]);useEffect(()=>{let mounted=true;adminGet<AdminPage>(`/api/admin/${mode}?page=1&size=100`).then(response=>{if(mounted)setItems(response.data.items)}).catch(error=>{if(mounted)setMessage(error instanceof Error?error.message:"加载失败")}).finally(()=>{if(mounted)setLoading(false)});return()=>{mounted=false}},[mode]);function change<K extends keyof FormState>(key:K,value:FormState[K]){setForm(current=>({...current,[key]:value}))}function openNew(){setForm(empty(mode));setEditing(true);setMessage("")}function openEdit(item:EditableItem){setForm(toForm(item,mode));setEditing(true);setMessage("");window.scrollTo({top:0,behavior:"smooth"})}
async function save(event:FormEvent){event.preventDefault();setSaving(true);setMessage("");let payload:Record<string,unknown>;if(mode==="posts")payload={title:form.title,slug:form.slug||null,summary:form.summary,content:form.content,coverImageUrl:form.coverImageUrl||null,tags:split(form.tags),series:form.series||null,status:form.status,featured:form.featured};else if(mode==="notes")payload={title:form.title,slug:form.slug||null,summary:form.summary,content:form.content,category:form.category||null,tags:split(form.tags),status:form.status};else payload={name:form.name,slug:form.slug||null,summary:form.summary,description:form.description,techStack:split(form.techStack),status:form.status,repoUrl:form.repoUrl||null,demoUrl:form.demoUrl||null,imageUrl:form.imageUrl||null,featured:form.featured,startDate:form.startDate||null,endDate:form.endDate||null};try{await adminMutation<EditableItem>(form.id?`/api/admin/${mode}/${form.id}`:`/api/admin/${mode}`,{method:form.id?"PATCH":"POST",body:JSON.stringify(payload)});setMessage(`${mode==="posts"?"文章":mode==="notes"?"笔记":"项目"}已保存`);setEditing(false);setForm(empty(mode));await load()}catch(error){setMessage(error instanceof Error?error.message:"保存失败")}finally{setSaving(false)}}
async function remove(item:EditableItem){const name=item.title||item.name||"这条内容";if(!confirm(`确定删除「${name}」吗？此操作不可撤销。`))return;try{await adminMutation<string>(`/api/admin/${mode}/${item.id}`,{method:"DELETE"});setMessage("内容已删除");await load()}catch(error){setMessage(error instanceof Error?error.message:"删除失败")}}async function publish(item:EditableItem){if(mode!=="posts"||!item.id)return;try{await adminMutation<EditableItem>(`/api/admin/posts/${item.id}/publish`,{method:"POST"});setMessage("文章已发布");await load()}catch(error){setMessage(error instanceof Error?error.message:"发布失败")}}
return <><div className="admin-heading"><div><h1>{config.title}</h1><p>{config.description}</p></div><button className="button primary small" onClick={openNew}>{config.newLabel}</button></div>{message&&<p className={`form-message ${message.includes("失败")||message.includes("错误")?"error":"success"}`}>{message}</p>}{editing&&<form className="admin-panel editor-panel" onSubmit={save}><div className="panel-head" style={{margin:"-22px -22px 22px"}}><h2>{form.id?"编辑内容":config.newLabel}</h2><button className="button small" type="button" onClick={()=>setEditing(false)}>关闭</button></div><div className="editor-grid">{mode==="projects"?<Field label="项目名称" required value={form.name} onChange={v=>change("name",v)}/>:<Field label={mode==="posts"?"文章标题":"笔记标题"} required value={form.title} onChange={v=>change("title",v)}/>}<Field label="Slug（留空自动生成）" value={form.slug} onChange={v=>change("slug",v)}/><div className="form-field span-2"><label>摘要</label><textarea className="form-control" value={form.summary} onChange={e=>change("summary",e.target.value)} maxLength={500}/></div>{mode==="posts"&&<><Field label="标签（逗号分隔）" value={form.tags} onChange={v=>change("tags",v)}/><Field label="系列" value={form.series} onChange={v=>change("series",v)}/><Field label="封面图片 URL" value={form.coverImageUrl} onChange={v=>change("coverImageUrl",v)}/></>}{mode==="notes"&&<><Field label="分类" value={form.category} onChange={v=>change("category",v)}/><Field label="标签（逗号分隔）" value={form.tags} onChange={v=>change("tags",v)}/></>}{mode==="projects"&&<><Field label="技术栈（逗号分隔）" value={form.techStack} onChange={v=>change("techStack",v)}/><Field label="仓库 URL" value={form.repoUrl} onChange={v=>change("repoUrl",v)}/><Field label="演示 URL" value={form.demoUrl} onChange={v=>change("demoUrl",v)}/><Field label="图片 URL" value={form.imageUrl} onChange={v=>change("imageUrl",v)}/><Field label="开始日期" type="date" value={form.startDate} onChange={v=>change("startDate",v)}/><Field label="结束日期" type="date" value={form.endDate} onChange={v=>change("endDate",v)}/></>}<div className="form-field"><label>状态</label><select className="form-control" value={form.status} onChange={e=>change("status",e.target.value)}>{(mode==="projects"?["ACTIVE","COMPLETED","MAINTAINED","EXPERIMENTAL","ARCHIVED"]:["DRAFT","PUBLISHED","SCHEDULED","ARCHIVED"]).map(status=><option key={status}>{status}</option>)}</select></div>{mode!=="notes"&&<label className="check-field"><input type="checkbox" checked={form.featured} onChange={e=>change("featured",e.target.checked)}/>在首页精选展示</label>}<div className="form-field span-2"><label>{mode==="projects"?"项目介绍（Markdown）":"正文（Markdown）"}</label><textarea ref={editorRef} className="form-control content-editor" required={mode!=="projects"} value={mode==="projects"?form.description:form.content} onChange={e=>change(mode==="projects"?"description":"content",e.target.value)}/></div></div><div className="editor-actions"><button className="button small" type="button" onClick={()=>setEditing(false)}>取消</button>{pasteUploading&&<span style={{color:"var(--accent-strong)",fontSize:12}}>正在上传粘贴的图片…</span>}<button className="button primary small" type="submit" disabled={saving}>{saving?"保存中…":"保存内容"}</button></div></form>}<section className="admin-panel"><div className="panel-head"><h2>全部内容</h2><span style={{color:"var(--text-3)",fontSize:12}}>{items.length} 条记录</span></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>标题</th><th>状态</th><th>{mode==="projects"?"技术栈":"Slug"}</th><th>更新/时间</th><th>操作</th></tr></thead><tbody>{loading?<tr><td colSpan={5}>正在加载…</td></tr>:items.length===0?<tr><td colSpan={5}>暂无内容，点击右上角创建第一条记录。</td></tr>:items.map(item=><tr key={item.id}><td><strong>{item.title||item.name}</strong><br/><span style={{color:"var(--text-3)",fontSize:11}}>{item.summary?.slice(0,60)}</span></td><td><span className={`status-badge ${item.status.toLowerCase()}`}>{item.status}</span></td><td>{mode==="projects"?item.techStack:item.slug}</td><td>{item.updatedAt?new Date(item.updatedAt).toLocaleDateString("zh-CN"):item.startDate||"—"}</td><td><div className="table-actions"><button onClick={()=>openEdit(item)}>编辑</button>{mode==="posts"&&item.status!=="PUBLISHED"&&<button onClick={()=>publish(item)}>发布</button>}<button onClick={()=>remove(item)}>删除</button></div></td></tr>)}</tbody></table></div></section></>}
function Field({label,value,onChange,required=false,type="text"}:{label:string;value:string;onChange:(value:string)=>void;required?:boolean;type?:string}){return <div className="form-field"><label>{label}</label><input className="form-control" type={type} required={required} value={value} onChange={e=>onChange(e.target.value)}/></div>}
